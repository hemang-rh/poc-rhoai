apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: model-tokenizer-pvc
  labels:
    app.kubernetes.io/name: model-tokenizer-pvc
    opendatahub.io/dashboard: 'true'
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi